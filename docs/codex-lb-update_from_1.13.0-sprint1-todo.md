# codex-lb / Sprint 1 — Atomic TODO with Artifacts

- [x] T1 Capture non-secret repository baseline | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/baseline/repo-baseline.txt` exists and contains `git branch --show-current`, `git rev-parse --short HEAD`, `git remote -v`, `git status --short`; `git diff --check` succeeds | *Зависит от:* Нет
- [x] T2 Preserve current non-doc worktree diff outside Git | *Артефакт:* `test -s /home/vgoro/codex-lb-backups/<timestamp>/patches/pre-upgrade-non-doc.patch && git diff --check` succeeds; patch covers `app/modules/proxy/service.py`, `openspec/specs/responses-api-compat/spec.md`, `tests/unit/test_proxy_utils.py` if still modified | *Зависит от:* T1
- [x] T3 Capture current Docker runtime baseline | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/baseline/docker-runtime.txt` exists and records `docker compose -f docker-compose.yml ps`, container images for `codex-lb-server-1` and `codex-lb-frontend-1`, plus current `/health` and `/health/ready` results | *Зависит от:* T1
- [x] T4 Capture redacted environment key inventory | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/baseline/env-keys-redacted.txt` exists, contains key names only, and does not contain raw `SECRET`, `TOKEN`, `KEY`, or password values | *Зависит от:* T3
- [x] T5 Back up production Docker data volume | *Артефакт:* `tar tzf /home/vgoro/codex-lb-backups/<timestamp>/localai_codex-lb-data.tgz >/dev/null` succeeds and backup is outside `/home/vgoro/codex-lb/docs` | *Зависит от:* T3, T4
- [x] T6 Inventory upstream 1.15 migration delta | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/baseline/migration-delta-v1.15.0.txt` exists and lists `v1.13.0..v1.15.0` migration/model changes; no project code is modified | *Зависит от:* T5
- [x] T7 Verify required upstream refs are available | *Артефакт:* `git rev-parse --verify refs/tags/v1.15.0 && git rev-parse --verify refs/remotes/upstream/pr/498` succeeds | *Зависит от:* T1
- [x] T8 Create isolated upgrade branch | *Артефакт:* `git branch --show-current` returns `upgrade/from-1.13.0-to-1.15.0` and `git status --short` still shows only expected pre-sprint changes | *Зависит от:* T2, T7
- [x] T9 Merge upstream v1.15.0 into upgrade branch | *Артефакт:* `git merge --no-ff v1.15.0` has completed and `git status --porcelain=v1 --unmerged` is empty | *Зависит от:* T8
- [x] T10 Align backend and frontend version markers to 1.15.0 | *Артефакт:* `grep -n "1.15.0" pyproject.toml app/__init__.py frontend/package.json && git diff --check -- pyproject.toml app/__init__.py frontend/package.json` succeeds | *Зависит от:* T9
- [x] T11 Reconcile proxy service with the pre-text `stream_incomplete` retry hotfix | *Артефакт:* `uv run pytest tests/unit/test_proxy_utils.py -q && uvx ruff check app/modules/proxy/service.py tests/unit/test_proxy_utils.py && uvx ruff format --check app/modules/proxy/service.py tests/unit/test_proxy_utils.py` succeeds | *Зависит от:* T9
- [x] T12 Reconcile OpenSpec response compatibility notes with the retry behavior | *Артефакт:* `grep -n "stream_incomplete" openspec/specs/responses-api-compat/spec.md && uvx ruff check tests/unit/test_proxy_utils.py && git diff --check -- openspec/specs/responses-api-compat/spec.md tests/unit/test_proxy_utils.py` succeeds | *Зависит от:* T11
- [x] T13 Prove PR #498 image API code is not included in Sprint 1 | *Артефакт:* `test ! -e app/core/openai/images.py && test ! -e app/modules/proxy/images_service.py && ! grep -R "/v1/images" -n app tests openspec` succeeds | *Зависит от:* T9
- [x] T14 Select and validate the Docker compose strategy for the observed two-service runtime | *Артефакт:* `docker compose -f docker-compose.yml config >/dev/null && grep -R "localai_codex-lb-data" docker-compose*.yml` succeeds; `server` and `frontend` services remain addressable | *Зависит от:* T9, T10
- [x] T15 Verify healthcheck endpoints are represented consistently in compose/runtime notes | *Артефакт:* `grep -R "/health/live\|/health/ready\|/health" docker-compose*.yml DEPLOY.md README.md && docker compose -f docker-compose.yml config >/dev/null` succeeds | *Зависит от:* T14
- [x] T16 Run migration-head and startup migration compatibility tests | *Артефакт:* `uv run pytest tests/integration/test_migrations.py tests/unit/test_db_migrate.py -q && uvx ruff check app/db tests/integration/test_migrations.py tests/unit/test_db_migrate.py && uvx ruff format --check app/db tests/integration/test_migrations.py tests/unit/test_db_migrate.py` succeeds | *Зависит от:* T10, T14
- [x] T17 Run request-log schema compatibility tests | *Артефакт:* `uv run pytest tests/unit/test_request_logs_repository.py -q && uvx ruff check app/modules/request_logs tests/unit/test_request_logs_repository.py && uvx ruff format --check app/modules/request_logs tests/unit/test_request_logs_repository.py` succeeds | *Зависит от:* T16
- [x] T18 Run accounts, settings, and API-key compatibility tests | *Артефакт:* `uv run pytest tests/integration/test_accounts_api.py tests/integration/test_settings_api.py tests/integration/test_api_keys_api.py -q && uvx ruff check app/modules/accounts app/modules/settings app/modules/api_keys tests/integration/test_accounts_api.py tests/integration/test_settings_api.py tests/integration/test_api_keys_api.py` succeeds | *Зависит от:* T16
- [x] T19 Verify GPT-5.5 model registry support without image routes | *Артефакт:* `grep -R "gpt-5.5" app/core/openai tests && uv run pytest tests/integration/test_openai_compat_features.py -q && uvx ruff check app/core/openai tests/integration/test_openai_compat_features.py` succeeds | *Зависит от:* T13, T18
- [x] T20 Run complete backend quality gate | *Артефакт:* `uv run pytest tests/unit tests/integration -q && uvx ruff check . && uvx ruff format --check . && uv run ty check` succeeds | *Зависит от:* T11, T12, T17, T19
- [x] T21 Run complete frontend quality gate | *Артефакт:* `cd frontend && bun install --frozen-lockfile && bun run lint && bun run typecheck && bun run test && bun run build` succeeds | *Зависит от:* T10, T14
- [x] T22 Build upgraded Docker services without deleting volumes | *Артефакт:* `docker compose -f docker-compose.yml build server frontend && docker compose -f docker-compose.yml config >/dev/null` succeeds; no `down -v` or volume deletion was used | *Зависит от:* T20, T21
- [x] T23 Start upgraded stack on preserved data volume | *Артефакт:* `docker compose -f docker-compose.yml up -d server frontend && docker compose -f docker-compose.yml ps` succeeds and `localai_codex-lb-data` is still the mounted data volume | *Зависит от:* T5, T22
- [x] T24 Verify live and ready health endpoints | *Артефакт:* `curl -fsS http://127.0.0.1:2455/health/live && curl -fsS http://127.0.0.1:2455/health/ready` succeeds after startup | *Зависит от:* T23
- [x] T25 Run minimal authenticated API smoke without printing secrets | *Артефакт:* `/home/vgoro/codex-lb-backups/20260503-230311/smoke/api-smoke-redacted.log` exists and shows successful `/v1/models` plus one proxy smoke; API key is not printed in shell history or logs | *Зависит от:* T24
- [x] T26 Capture post-smoke stability logs and container state | *Артефакт:* `/home/vgoro/codex-lb-backups/20260503-230311/smoke/post-smoke-docker-state.txt` exists and includes `docker compose -f docker-compose.yml ps` plus `docker compose -f docker-compose.yml logs --tail=200 server frontend`; no crash loops are present | *Зависит от:* T25
- [x] T27 Document rollback path before Sprint 2 cutover | *Артефакт:* `/home/vgoro/codex-lb-backups/20260503-230311/rollback.md` exists and records previous commit, upgraded commit, image tags/IDs, backup archive path, and restore command outline without secrets | *Зависит от:* T26
- [x] T28 Produce final Sprint 1 evidence bundle | *Артефакт:* `/home/vgoro/codex-lb-backups/20260503-230311/sprint1-evidence.md` exists and links baseline, backup verification, backend gate, frontend gate, Docker smoke, and rollback artifacts; `git diff --check` succeeds | *Зависит от:* T20, T21, T24, T25, T27
- [x] T_FINAL Завершение спринта и обновление документации | *Артефакт:* Статус спринта в документации переведен в "Completed", проверено, что все остальные задачи имеют статус [x], и эти изменения зафиксированы чистым финальным docs-коммитом. | *Зависит от:* Всех предыдущих задач
