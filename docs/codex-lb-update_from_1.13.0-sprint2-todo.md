- [x] T1 Confirm Sprint 1 completion and current repository state | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/baseline/repo-state.txt` contains branch, HEAD, remotes, `git status --short --branch`, and Sprint 1 checklist/TODO completion summary | *Зависит от:* Нет
- [x] T2 Confirm PR #498 image API remains excluded from the 1.15.0 cutover baseline | *Артефакт:* `test ! -e app/core/openai/images.py && test ! -e app/modules/proxy/images_service.py && ! grep -R "/v1/images\|images/generations\|gpt-image" -n app tests openspec` succeeds | *Зависит от:* T1
- [x] T3 Capture active Docker containers, images, and mounts before cutover | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/baseline/docker-runtime.txt` contains `docker compose -f docker-compose.yml ps`, image IDs, and mounts for `codex-lb-server-1` and `codex-lb-frontend-1` | *Зависит от:* T1
- [x] T4 Confirm preserved runtime volume before any restart | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/baseline/docker-runtime.txt` shows `localai_codex-lb-data` mounted at `/var/lib/codex-lb` and no new replacement data volume | *Зависит от:* T3
- [x] T5 Capture pre-cutover health baseline in the required order | *Артефакт:* `curl -fsS http://127.0.0.1:2455/health/live && curl -fsS http://127.0.0.1:2455/health/ready` succeeds and responses are saved to `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/baseline/health-before.txt` | *Зависит от:* T3
- [x] T6 Create a fresh read-only backup of `localai_codex-lb-data` outside Git | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/localai_codex-lb-data.tgz` exists and was created with `docker run --rm -v localai_codex-lb-data:/data:ro ... tar czf ...` | *Зависит от:* T4
- [x] T7 Verify the fresh volume backup archive | *Артефакт:* `tar tzf /home/vgoro/codex-lb-backups/<timestamp>/sprint2/localai_codex-lb-data.tgz >/dev/null && sha256sum /home/vgoro/codex-lb-backups/<timestamp>/sprint2/localai_codex-lb-data.tgz` succeeds and checksum is recorded | *Зависит от:* T6
- [x] T8 Validate live compose syntax for the selected two-service runtime | *Артефакт:* `docker compose -f docker-compose.yml config >/dev/null && git diff --check -- docker-compose.yml` succeeds | *Зависит от:* T1
- [x] T9 Decide and document prod compose cutover status without deploying the stale prod file | *Артефакт:* `docker compose -f docker-compose.prod.yml config >/dev/null && git diff --check -- docker-compose.prod.yml "docker-compose.prod copy.yml" DEPLOY.md README.md docs/codex-lb-update_from_1.13.0-sprint2-checklist.md` succeeds, or docs explicitly mark prod compose as non-cutover/stale | *Зависит от:* T8
- [x] T10 Verify healthcheck semantics in compose and docs | *Артефакт:* `grep -R "/health/ready" -n docker-compose.yml docker-compose.prod.yml DEPLOY.md README.md docs/codex-lb-update_from_1.13.0-sprint2-checklist.md && export PATH="/tmp/codex-lb-uv-tool/bin:$PATH" && uvx ruff check app/modules/health` succeeds | *Зависит от:* T8
- [x] T11 Confirm all 1.15.0 version markers before image build | *Артефакт:* `grep -n "version = \"1.15.0\"" pyproject.toml && grep -n "1.15.0" app/__init__.py frontend/package.json` succeeds | *Зависит от:* T2, T9, T10
- [x] T12 Build final server and frontend images without deleting volumes | *Артефакт:* `docker compose -f docker-compose.yml build server frontend` succeeds and no `docker volume rm`, `docker compose down -v`, or `docker system prune --volumes` was used | *Зависит от:* T7, T11
- [x] T13 Record final image IDs, digests, and sizes | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/build/image-digests.txt` contains `codex-lb-server:latest` and `codex-lb-frontend:latest` IDs/digests/sizes | *Зависит от:* T12
- [x] T14 Start only `server` and `frontend` on the preserved volume | *Артефакт:* `docker compose -f docker-compose.yml up -d server frontend && docker compose -f docker-compose.yml ps` succeeds and only the codex-lb stack is recreated/started | *Зависит от:* T7, T13
- [x] T15 Verify post-start container mounts and running state | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/cutover/post-start-containers.txt` shows `codex-lb-server-1`, `codex-lb-frontend-1`, and `localai_codex-lb-data -> /var/lib/codex-lb` | *Зависит от:* T14
- [x] T16 Validate post-cutover `/health/live` then `/health/ready` | *Артефакт:* `curl -fsS http://127.0.0.1:2455/health/live && curl -fsS http://127.0.0.1:2455/health/ready` succeeds and `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/cutover/health-after.txt` records restart attempts count | *Зависит от:* T15
- [x] T17 Capture logs before any retry if readiness fails, otherwise record no retry needed | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/cutover/readiness-retry.log` contains either redacted server logs plus retry count no greater than 2, or `READY_OK_NO_RETRY` | *Зависит от:* T16
- [x] T18 Verify dashboard settings and account metadata survived cutover | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/smoke/dashboard-settings-accounts-redacted.json` contains non-secret counts/metadata and no OAuth token JSON, API key, account export, or encryption key material | *Зависит от:* T16
- [x] T19 Run account/settings backend tests and linters | *Артефакт:* `export PATH="/tmp/codex-lb-uv-tool/bin:$PATH" && uv run pytest tests/integration/test_accounts_api.py tests/integration/test_settings_api.py -q && uvx ruff check app/modules/accounts app/modules/settings tests/integration/test_accounts_api.py tests/integration/test_settings_api.py` succeeds | *Зависит от:* T18
- [x] T20 Verify API-key metadata and authenticated `/v1/models` smoke without exposing keys | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/smoke/models-smoke-redacted.json` contains status code/model count only and `! grep -R "sk-clb\|Bearer " /home/vgoro/codex-lb-backups/<timestamp>/sprint2/smoke/models-smoke-redacted.json` succeeds | *Зависит от:* T16
- [x] T21 Run API-key/auth/model backend tests and linters | *Артефакт:* `export PATH="/tmp/codex-lb-uv-tool/bin:$PATH" && uv run pytest tests/integration/test_api_keys_api.py tests/integration/test_auth_middleware.py tests/integration/test_v1_models.py -q && uvx ruff check app/modules/api_keys app/core/auth app/modules/proxy tests/integration/test_api_keys_api.py tests/integration/test_auth_middleware.py tests/integration/test_v1_models.py` succeeds | *Зависит от:* T20
- [x] T22 Run one minimal authenticated `/v1/chat/completions` smoke with redacted output | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/smoke/chat-completions-smoke-redacted.json` contains status/object/model/usage metadata only and secret scan for `sk-clb` and `Bearer ` passes | *Зависит от:* T20
- [x] T23 Verify request logs and usage surfaces after proxy smoke | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/smoke/request-logs-usage-redacted.json` shows non-secret request-log and usage summary metadata for the smoke request | *Зависит от:* T22
- [x] T24 Run request-log/usage backend tests and linters | *Артефакт:* `export PATH="/tmp/codex-lb-uv-tool/bin:$PATH" && uv run pytest tests/integration/test_request_logs_api.py tests/integration/test_request_logs_filters.py tests/integration/test_usage_api.py tests/integration/test_v1_usage.py tests/unit/test_request_logs_repository.py -q && uvx ruff check app/modules/request_logs app/modules/usage tests/integration/test_request_logs_api.py tests/integration/test_request_logs_filters.py tests/integration/test_usage_api.py tests/integration/test_v1_usage.py tests/unit/test_request_logs_repository.py` succeeds | *Зависит от:* T23
- [x] T25 Verify frontend status/version and run frontend quality gate | *Артефакт:* `cd frontend && bun run lint && bun run typecheck && bun run test` succeeds, and `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/smoke/frontend-version-redacted.txt` confirms the UI/status artifact reports version `1.15.0` | *Зависит от:* T16
- [x] T26 Capture post-cutover Docker state and redacted logs | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/post-cutover-state.txt` contains `docker compose -f docker-compose.yml ps` and redacted `logs --tail=200 server frontend`, with no crash loops | *Зависит от:* T17, T22, T23, T25
- [x] T27 Scan all Sprint 2 runtime artifacts for secrets | *Артефакт:* `! grep -R "sk-clb\|Bearer \|OPENAI_OAUTH\|CODEX_LB_ENCRYPTION_KEY" /home/vgoro/codex-lb-backups/<timestamp>/sprint2` succeeds, or every match is documented as a false positive/redacted placeholder | *Зависит от:* T26
- [x] T28 Document non-destructive Sprint 2 rollback path | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/rollback-sprint2.md` records commits, image IDs/digests, preserved volume, backup path, live/ready verification, and states backup restore is last resort after a separate safety copy | *Зависит от:* T7, T13, T27
- [x] T29 Produce Sprint 2 evidence bundle | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint2/sprint2-evidence.md` links baseline, backup, compose validation, build, cutover, health, dashboard/API checks, proxy smoke, tests/linters, logs, rollback, and secret-scan results | *Зависит от:* T19, T21, T24, T25, T27, T28
- [x] T30 Verify no unintended dirty code remains before completion | *Артефакт:* `git diff --check && git status --short` is reviewed, `git diff --exit-code -- uv.lock` succeeds unless an approved task required lockfile changes, and only intentional Sprint 2 docs/runtime-evidence references are staged for the final docs commit | *Зависит от:* T29
- [x] T_FINAL Завершение спринта и обновление документации | *Артефакт:* Статус спринта в документации переведен в "Completed", проверено, что все остальные задачи имеют статус [x], и эти изменения зафиксированы чистым финальным docs-коммитом. | *Зависит от:* Всех предыдущих задач
