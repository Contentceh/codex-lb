# codex-lb / Sprint 5 TODO — атомарные задачи с артефактами

- [x] T1 Создать каталог evidence для Sprint 5 | *Артефакт:* `test -d /home/vgoro/codex-lb-backups/<timestamp>/sprint5 && test -d /home/vgoro/codex-lb-backups/<timestamp>/sprint5/baseline && test -d /home/vgoro/codex-lb-backups/<timestamp>/sprint5/research && test -d /home/vgoro/codex-lb-backups/<timestamp>/sprint5/tests` | *Зависит от:* Нет
- [x] T2 Зафиксировать наличие upstream v1.16 refs | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/baseline/release-availability.txt` содержит вывод `git ls-remote --tags upstream 'refs/tags/v1.16*'` и явно отмечает `release_availability_gate=PASS` или `release_availability_gate=BLOCKED` | *Зависит от:* T1
- [x] T3 Зафиксировать git-baseline live-ветки | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/baseline/git-baseline.txt` содержит `git status --short --branch`, `git rev-parse HEAD`, `git remote -v`; проверено, что вне docs/evidence нет неожиданного dirty-кода | *Зависит от:* T1
- [x] T4 Зафиксировать Docker runtime baseline | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/baseline/docker-runtime.txt` содержит sanitized `docker ps` для `codex-lb-server-1` и `codex-lb-frontend-1`, без secrets и без volume-destructive команд | *Зависит от:* T1
- [x] T5 Проверить live health baseline | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/baseline/health-baseline.txt` содержит успешные проверки `curl -fsS -o /dev/null -w 'health_live=%{http_code}\n' http://127.0.0.1:2455/health/live` и затем `curl -fsS -o /dev/null -w 'health_ready=%{http_code}\n' http://127.0.0.1:2455/health/ready` | *Зависит от:* T4
- [x] T6 Зафиксировать sanitized image/runtime settings | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/baseline/runtime-settings-presence.txt` содержит только наличие/отсутствие `CODEX_LB_IMAGES_DEFAULT_MODEL`, `CODEX_LB_IMAGES_HOST_MODEL`, `CODEX_LB_IMAGES_MAX_PARTIAL_IMAGES`, без значений секретов | *Зависит от:* T1
- [x] T7 Обновить Sprint 5 release blocker в документации при отсутствии v1.16 | *Артефакт:* `grep -E 'v1\.16.*BLOCKED|release_availability_gate=BLOCKED' docs/codex-lb-update_from_1.13.0-roadmap.md docs/codex-lb-update_from_1.13.0-blockers.md` находит явный blocker; `git diff --check` проходит | *Зависит от:* T2
- [x] T8 Собрать upstream main diffstat и log | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/upstream-main-diffstat.txt` и `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/upstream-main-log.txt` созданы командами `git diff --stat v1.15.0..upstream/main` и `git log --oneline --decorate v1.15.0..upstream/main` | *Зависит от:* T2
- [x] T9 Собрать v1.16 diffstat и log, если upstream ref существует | *Артефакт:* если ref есть — созданы `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/v1.16-diffstat.txt` и `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/v1.16-log.txt`; если ref отсутствует — artifact содержит `v1.16_candidate=BLOCKED_BY_NO_UPSTREAM_REF` | *Зависит от:* T2
- [x] T10 Собрать targeted changed-file list для backend risk surface | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/backend-risk-files.txt` содержит изменённые пути для proxy, images, request_logs, usage, api_keys, accounts/OAuth, migrations; без secrets и без request bodies | *Зависит от:* T8
- [x] T11 Собрать targeted changed-file list для frontend/docker risk surface | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/frontend-docker-risk-files.txt` содержит изменённые пути для `frontend/`, `deploy`, compose/Docker files и package manifests; без secrets | *Зависит от:* T8
- [x] T12 Создать upstream delta map | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/upstream-delta-map.md` содержит разделы proxy, `/v1/images/*`, request logs, usage/pricing, API keys, accounts/OAuth, migrations, Docker/compose, frontend и marker `delta_map_gate=PASS` | *Зависит от:* T8, T9, T10, T11
- [x] T13 Сравнить local image branch с upstream/pr/498 | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/pr498-diffstat.txt` и `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/pr498-log.txt` созданы сравнением `feature/pr-498-images-api-on-1.15.0...upstream/pr/498` без изменения кода | *Зависит от:* T3
- [x] T14 Собрать review по image routes/schemas/accounting | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/image-surface-review.md` описывает `/v1/images/generations`, `/v1/images/edits`, `/v1/images/variations`, schema validation, transport, request logs, usage/cost, allowed_models и не содержит `b64_json` или generated images | *Зависит от:* T13
- [x] T15 Создать PR #498 drop/rebase/retain matrix | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/pr498-alignment.md` содержит решения `drop`, `rebase` или `retain` по patch chunks и marker `patch_alignment_gate=PASS`; `git diff --check` проходит | *Зависит от:* T12, T14
- [x] T16 Зафиксировать candidate branch policy | *Артефакт:* `docs/codex-lb-update_from_1.13.0-roadmap.md` или `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/candidate-branch-policy.md` содержит branch pattern, clean-worktree/stash правило и запрет fake 1.16 candidate; `git diff --check` проходит | *Зависит от:* T7, T15
- [x] T17 Зафиксировать conflict-resolution и stop rules | *Артефакт:* `docs/codex-lb-update_from_1.13.0-blockers.md` или `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/conflict-resolution-policy.md` содержит приоритеты upstream 1.16 → live-data preservation → local compatibility и стоп-условия по migrations/Docker/OAuth/image accounting; `git diff --check` проходит | *Зависит от:* T16
- [x] T18 Зафиксировать uv.lock policy для Sprint 5 | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/research/uv-lock-policy.md` содержит правило допустимости `uv.lock` только при явном upstream dependency change; `git diff --exit-code -- uv.lock && git diff --cached --exit-code -- uv.lock` проходят до начала candidate coding | *Зависит от:* T16
- [x] T19 Подготовить backend candidate gate command bundle | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/tests/backend-candidate-gates.txt` содержит команды `export PATH="/tmp/codex-lb-uv-tool/bin:$PATH"`, targeted `uv run pytest ... -q`, `uvx ruff check ...`, `git diff --check`, `git diff --cached --check`, `git diff --exit-code -- uv.lock`, `git diff --cached --exit-code -- uv.lock`; без запуска live cutover | *Зависит от:* T12, T15, T18
- [x] T20 Выполнить backend candidate gates при наличии candidate ref | *Артефакт:* если candidate ref есть — `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/tests/backend-candidate-results.txt` содержит успешные `uv run pytest ... -q && uvx ruff check ... && git diff --check && git diff --cached --check && git diff --exit-code -- uv.lock && git diff --cached --exit-code -- uv.lock`; если ref отсутствует — artifact содержит `backend_candidate_gates=BLOCKED_BY_NO_UPSTREAM_REF` | *Зависит от:* T19
- [x] T21 Подготовить frontend/dashboard compatibility review | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/tests/frontend-dashboard-review.md` описывает accounts, settings, API keys, request logs, usage summaries и model options against upstream changes; без secrets и без dirty-кода | *Зависит от:* T11, T12
- [x] T22 Выполнить frontend candidate gates при наличии candidate ref | *Артефакт:* если candidate ref есть — `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/tests/frontend-candidate-gates.txt` содержит успешные `cd frontend && bun run lint && bun run typecheck && bun run test && bun run build`; если ref/tools отсутствуют — artifact содержит явный blocker и issue не отмечается complete | *Зависит от:* T21
- [x] T23 Подготовить rollback command notes | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/rollback-notes.md` содержит build/restart/rollback команды для observed `server` + `frontend` stack, без `docker volume rm`, `docker compose down -v`, `docker system prune --volumes`; `grep -E 'volume rm|down -v|system prune --volumes' rollback-notes.md` не находит запрещённых команд | *Зависит от:* T4, T16
- [x] T24 Подготовить live rehearsal smoke plan | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/tests/live-rehearsal-plan.txt` содержит порядок `/health/live`, `/health/ready`, dashboard/API, authenticated text proxy smoke, `/v1/models`, request logs/usage, затем максимум one non-streaming и one streaming `gpt-image-2` request только после approval | *Зависит от:* T5, T23
- [x] T25 Зафиксировать live rehearsal stop criteria | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/tests/live-rehearsal-stop-criteria.md` содержит stop conditions для migrations, missing accounts/settings, model-scope failures, public/private model leaks, health regressions и marker `rollback_plan_gate=PASS` | *Зависит от:* T23, T24
- [x] T26 Собрать Sprint 5 evidence summary | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/sprint5-evidence.md` содержит summary release availability, delta map, PR #498 matrix, branch policy, validation matrix, rollback plan и markers `release_availability_gate=PASS|BLOCKED`, `delta_map_gate=PASS`, `patch_alignment_gate=PASS`, `validation_matrix_gate=PASS`, `rollback_plan_gate=PASS` | *Зависит от:* T2, T12, T15, T16, T19, T20, T22, T25
- [x] T27 Выполнить suppressed secret scan Sprint 5 artifacts | *Артефакт:* `/home/vgoro/codex-lb-backups/<timestamp>/sprint5/secret-scan-results.txt` содержит `secret_scan=PASS`; скан не печатает matched line contents и подтверждает отсутствие raw API keys, OAuth token JSON, `.env.local`, DB dumps, encryption key material, full Authorization headers, generated binary images, `b64_json` | *Зависит от:* T26
- [x] T28 Проверить финальные docs/uv/git gates | *Артефакт:* `git diff --check && git diff --cached --check && git diff --exit-code -- uv.lock && git diff --cached --exit-code -- uv.lock && git status --short --branch` проходят и показывают только ожидаемые docs/evidence изменения без dirty project code | *Зависит от:* T27
- [x] T29 Зафиксировать Sprint 5 docs/evidence commit | *Артефакт:* `git log -1 --oneline` показывает финальный docs/evidence commit Sprint 5, а `git status --short --branch` чистый или содержит только осознанно незакоммиченные external evidence files вне repo | *Зависит от:* T28
- [ ] T_FINAL Завершение спринта и обновление документации | *Артефакт:* Статус спринта в документации переведен в "Completed", проверено, что все остальные задачи имеют статус [x], и эти изменения зафиксированы чистым финальным docs-коммитом. | *Зависит от:* Всех предыдущих задач
